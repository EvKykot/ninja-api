import { BeltLevelMiddleware } from './belt-level.middleware';

describe('BeltLevelMiddleware', () => {
  it('should be defined', () => {
    expect(new BeltLevelMiddleware()).toBeDefined();
  });
});
